# Media assets

Drop dashboard screenshots / GIF here so the README can display them:

- `dashboard.png` — main dashboard view (live tiles, charts)
- `dashboard.gif` — short screen recording of a live shot
- `pressure-profile.png` — pressure profile editor (optional)
